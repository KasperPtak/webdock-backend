<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Template</title>
    <style>
        /* Add your CSS styling here */
    </style>
</head>
<body>
    <h1>Posts Updated in the Last 24 Hours</h1>
    <% for (let i = 0; i < posts.length; i++) { %>
        <p><%= posts[i].title %></p>
        <p><%= posts[i].content %></p>
        <a href="https://localhost:8080/post/<%= posts[i].id %> ">go to post </a>
        <!-- created at/updated at here -->
        
        <hr /> 
    <% } %>
</body>
</html>
